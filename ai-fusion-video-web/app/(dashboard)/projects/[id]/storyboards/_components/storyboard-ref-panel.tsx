/**
 * 分镜引用面板出口(对外导入路径保持不变)。
 * 实现按 UI 职责拆分在同目录 ref-panel/ 下。
 */
export { StoryboardRefPanel } from "./ref-panel/storyboard-ref-panel";
export type { BatchFrameGeneratePayload } from "./ref-panel/shared";
