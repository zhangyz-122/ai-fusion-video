# 创作 UI 第一批

关联总任务 B01/B03/B04/E01：以现有生成器为基础，新增 /generate 创作工作台和 /generate/images 图像工坊。顶部工具入口改为创作工作台，旧生图/视频路由保留。

图像工坊从真实模型API读取启用的图片模型，管理员从工作流分页API读取待接入项；普通用户不请求管理员接口。点击指定模型须准确传入既有生成器，不改变默认文戏配置。

本批不重做全部编辑器，不新增后台运行链，不把Klein目录登记视为完成生成。验收：生产构建、工作台和工坊可见、Klein待接入可见、既有模型进入编辑器正确。
