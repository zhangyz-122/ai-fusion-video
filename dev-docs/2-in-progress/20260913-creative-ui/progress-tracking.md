# 进度

## 第二批：图像编辑页

- 新增独立 ImageStudioHeader，提供返回工坊、资产中心、快捷/展开模式切换。
- 图片模式默认展开既有高级面板，视频模式原有初始化不变；保留仓库中其他视频导演台改动。
- 生产构建及TypeScript通过，已更新前端并重载nginx。
- 浏览器确认模型15正确选中、提示词/参考素材/输出参数可见、快捷模式切换成功、52条历史记录可读取。未提交生成任务；不宣称出图验证通过。

- 已实现创作工作台、图像工坊、导航关联及指定模型启动。
- 读取真实模型和工作流目录，提供加载/错误/空状态；权限不足时不读取管理员工作流。
- Docker前端生产构建通过（含TypeScript）；已仅更新frontend并重载nginx，未重启后端及Runtime。
- 浏览器已验证 /generate 工作台、/generate/images 工坊；真实显示两个启用模型及三个Klein待接入项。
- 点击参考图模型正确进入 /generate/image?modelId=15，编辑器选中模型15，原有52条创作记录仍可读取；未提交新生成任务。
- 已检查桌面截图；窄屏、普通用户账号和故障注入场景尚未实际验收，未验证生图产物。

## 2026-09-13 收尾验收（全部通过）

- 测试账号：通过临时开放 `allow_register` + 公开注册接口创建 `uitest`（仅 user 角色、单团队 MEMBER），注册开关已恢复关闭。
- 普通用户：工作台、图像工坊、生图编辑器正常；图像工坊仅显示 2 个启用模型，无 Klein 待接入项；编辑器整页 API 清单不含 `/api/ai/comfyui/workflow/*` 管理员接口；创作记录仅显示本人 1 条。
- 窄屏 390×844：工作台与图像工坊布局正常，导航折叠为汉堡菜单，卡片纵向堆叠无横向溢出。
- 故障注入：`docker stop fusion-backend` 后，图像工坊加载态转为错误态（"无法读取生图模型，请检查服务后重试"+重试按钮），编辑器显示"暂无可用模型"且生成按钮禁用；后端恢复后页面自动恢复正常，无白屏、无无限加载。
- 生图产物：uitest 使用模型14（minimax_h3_t8_donghua_still_model）真实生成 1024x1024 图片一张（"清晨山谷中的现代木屋"），产物可预览、下载、作为参考、添加资产，ComfyUI 全链路端到端可用。
- 迁移后环境验证：后端 71/71 选定测试通过、前端生产构建通过（记录于 platform-handoff/progress-tracking.md）。

## 遗留

- 普通用户的公共能力目录接口仍欠（见 technical-debt.md）。
- 验收过程中发现跨用户项目数据隔离漏洞，属平台 M01 问题，已记录于 20260913-platform-handoff/technical-debt.md，不在本批修复。
