# 技术债务

- 当前 V1 的“万能”是统一产品入口和参数契约，尚未把多个 RunningHub 工作流物理合并成一张 ComfyUI 执行图。
- 二采、SeedVR2、动作增强等能力不能仅靠前端开关宣称已接入；必须先有目标 ComfyUI 节点、模型依赖、输入输出绑定和在线试运行证据。
- 默认视频模型仍由管理员的 `defaultModel` 配置决定。若未设置默认模型，系统沿用启用列表首项，并在界面展示实际底座名称。
