spring:
  application:
    name: ai-fusion-video

  datasource:
    hikari:
      connection-init-sql: "SET time_zone = '${app.mysql.session-time-zone}'"

  threads:
    virtual:
      enabled: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  profiles:
    active: local

  # Flyway
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

# MyBatis-Plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: true
      logic-not-delete-value: false

# Swagger
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 18080
  forward-headers-strategy: framework

# 应用配置
app:
  # Java 时区使用 IANA Zone ID；MySQL 会话使用无需安装命名时区表的 UTC 偏移。
  time-zone: ${APP_TIME_ZONE:Asia/Shanghai}
  mysql:
    session-time-zone: ${MYSQL_SESSION_TIME_ZONE:+08:00}
  cors:
    # 默认全局允许跨域；生产环境可通过 CORS_ALLOWED_ORIGIN_PATTERNS 配置明确来源，多个值使用逗号分隔。
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:*}
  storage:
    local-base-path: ./data/media
  agent-workspace:
    local-base-path: ${AGENT_WORKSPACE_LOCAL_PATH:./data/agent-workspace}
  generation:
    reaper:
      # 滞留任务回收：排队/执行中超过该小时数的任务自动标记失败，并联动失败等待中的生产运行。
      stale-hours: 2
      # 回收器调度周期与首次启动延迟（毫秒）。
      fixed-delay-ms: 600000
      initial-delay-ms: 60000

video:
  compose:
    ffmpeg-path: ${VIDEO_COMPOSE_FFMPEG_PATH:ffmpeg}
    ffprobe-path: ${VIDEO_COMPOSE_FFPROBE_PATH:ffprobe}

fusion:
  agentscope:
    v2:
      cache:
        maximum-size: 64
        expire-after-access: 30m
        capacity-wait: 5s
      state:
        mode: mysql
        database-name: "ai_fusion_video"
        table-name: "afv_agent_state"
      ingress:
        max-events: 4096
        max-bytes: 8388608
        coalesce-delay: 50ms
        coalesce-max-chars: 1024
      execution:
        owner-lease: 30s
        run-timeout: 30m
        confirmation-timeout: 48h
        max-iters: 999
      skills:
        enabled: true
        fail-fast: true
        display-names:
          universal-director: 万能导演
          character-scene-bible: 角色与场景圣经
          storyboard-prompt-compiler: 分镜提示词编译器
          reference-consistency: 参考一致性
          video-qc-repair: 视频质检与修复
        repositories:
          bundled:
            location: classpath:agentscope/skills
            lazy: false
      mcp:
        enabled: ${AGENTSCOPE_MCP_ENABLED:false}
        fail-fast: true
        servers: {}
